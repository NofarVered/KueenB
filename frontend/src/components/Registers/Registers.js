import React, {Component } from 'react';

class Registers extends React.Component {
    render(){
        return(
            <div>
                Registers List
                </div>
    
        )
    }
}

export default Registers;
